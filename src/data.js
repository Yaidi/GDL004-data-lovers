/* Manejo de data */

// esta es una función de ejemplo
import { dataPokemon } from './main.js';
export const name = () => {
//  for(let i=0; i < dataPokemon.name.length; i++){
  /*let getNames = dataPokemon.map(function(x){
    return x.name*/
  let names = dataPokemon.filter(dataPokemon => dataPokemon.name = dataPokemon.name);
    console.log(names);
    createPokemon()
};
export const height = () => {
  let heights = dataPokemon.filter(dataPokemon => dataPokemon.height = dataPokemon.height);
  console.log(heights);
};
export const grass = () => {
  let grasses = dataPokemon.filter(dataPokemonpokemon => { dataPokemon.type.map(function(elements){
  if(element === "Grass"){
  return dataPokemon;}
})
} === "Grass");

  console.log(grasses);
};
function createPokemon(){

let name= "hola";
};
